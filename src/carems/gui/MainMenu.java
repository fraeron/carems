package carems.gui;

import javax.swing.JFrame;

public class MainMenu extends JFrame {
    public MainMenu(){  
        setTitle("Carems");
        setSize(800,600);  
        setLayout(null);  
        setLocationRelativeTo(null);
        setVisible(true);  
    }
}
